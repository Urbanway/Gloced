---
applications:
  .:
    name: gloced
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      gloced-mysql-43165:
        type: mysql
      mysql-dffa:
        type: mysql
